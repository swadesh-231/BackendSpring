spring:
  application:
    name: BackendSpring

  datasource:
    url: ${DB_URL}

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8084

#  profiles:
#    active: prod

app:
  message: "Hello from default profile"




